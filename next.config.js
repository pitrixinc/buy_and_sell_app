/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['firebasestorage.googleapis.com', 'assets.jiji.ng', 'lh3.googleusercontent.com'], // Add other domains if needed
    }
  };
  
  module.exports = nextConfig;
  